# Forward queue after the Corbis MCP research-focused README update (2026-08-14)

Candidate follow-up work. This is a menu, not an authorization.

## Public copy and assets

- **Approve a current landing-page hero screenshot** (confidence: verified
  blocker; priority: high)
  Capture or approve an image from the live Corbis page that contains no stale
  paper-count or capability claim, then add it to the README and source asset
  checks.

- **Reconcile any new public asset with Marketplace promotion** (confidence:
  verified requirement; priority: high)
  Before a signed source tag is promoted, have the Marketplace owner update its
  separate exact allowlist and test the staged payload. Do not edit a promoted
  snapshot from this repository.

## Documentation

- **Revalidate the user-facing README before public visibility** (confidence:
  verified requirement; priority: medium)
  Check every user-facing claim, link, badge, and status statement against the
  live product and the exact source-release state. Keep client acceptance and
  Marketplace status separate from endpoint health.
