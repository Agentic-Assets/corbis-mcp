# Forward queue after Corbis MCP v0.1.1 release-record correction (2026-08-14)

Candidate work surfaced while correcting the source release record. This is a
menu, not authorization. Revalidate each item against the current source and
Marketplace state before acting.

## Release process

- **Review and tag the corrected `0.1.2` candidate** (confidence: verified
  requirement; priority: high)
  The immutable `v0.1.1` payload cannot be edited. If the corrected package is
  intended for Marketplace distribution, review and merge this candidate.
  Then make the final release commit that changes the `0.1.2` changelog entry
  from `Unreleased` to its release date and removes the untagged-candidate
  state. Create the signed annotated tag only on that final release commit;
  do not move or replace `v0.1.1`.

- **Repromote and repeat exact-digest client acceptance** (confidence: verified
  requirement; priority: high)
  A `0.1.2` source tag will produce a new Marketplace payload digest. Collect
  fresh, redacted Claude Code, Codex CLI, and Cursor local evidence for that
  digest before admission and attestation; earlier endpoint checks or a
  `v0.1.1` preview cannot substitute.

## Documentation controls

- **Add a release-record consistency check** (confidence: verified gap;
  priority: medium)
  The static validator confirms descriptor-version agreement but does not
  compare the active descriptor version with changelog status or remote tags.
  Consider a maintenance-only check that distinguishes an untagged candidate
  from a tagged release without embedding mutable promotion facts in the
  source payload.

- **Keep historical closeouts immutable** (confidence: implemented practice;
  priority: medium)
  Older closeouts correctly record their observations at the time. Add dated
  correction records, as this branch does, rather than rewriting historical
  evidence to look current.
