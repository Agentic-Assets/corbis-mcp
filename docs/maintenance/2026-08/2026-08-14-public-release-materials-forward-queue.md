# Forward queue after Corbis MCP public release materials (2026-08-14)

Candidate follow-up work. This is a menu, not an authorization.

## Release

- **Review public brand-asset approval** (confidence: verified gate; priority:
  high)
  Confirm the existing icon and wordmark assets are authorized for public
  release before creating a source tag.

- **Obtain a signed source tag** (confidence: verified gate; priority: high)
  After source review and merge, use the configured trusted signing authority
  to create and verify an annotated final tag on a commit reachable from main.

## Security and support

- **Revalidate public contact ownership before release** (confidence: verified
  maintenance requirement; priority: medium)
  Confirm `corbis@agenticassets.ai` and `security@agenticassets.ai` still route
  to the intended teams before public visibility or directory submission.

## Marketplace and acceptance

- **Keep client proof and Marketplace admission separate** (confidence:
  verified requirement; priority: high)
  Obtain real Claude Code, Codex CLI, and Cursor local acceptance evidence for
  the exact signed source tag before digest-bound Marketplace proof, admission,
  and attestation work.
